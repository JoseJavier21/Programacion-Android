<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/navigation"
    app:startDestination="@id/listaFragment">

    <fragment
        android:id="@+id/listaFragment"
        android:name="com.example.ejercicioretrofit.ListaFragment"
        android:label="fragment_lista"
        tools:layout="@layout/fragment_lista" >
        <action
            android:id="@+id/action_listaFragment_to_persoFragment"
            app:destination="@id/persoFragment" />
    </fragment>
    <fragment
        android:id="@+id/persoFragment"
        android:name="com.example.ejercicioretrofit.PersoFragment"
        android:label="fragment_perso"
        tools:layout="@layout/fragment_perso" />
</navigation>